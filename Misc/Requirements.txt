# Core dependencies
opencv-python>=4.8.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
pandas>=2.0.0
openpyxl>=3.1.0

# YOLO and object detection
ultralytics>=8.0.0

# XML parsing
lxml>=4.9.0

# Progress bars
tqdm>=4.65.0

# Deep SORT (for tracking - if using Step 2.py)
deep-sort-realtime>=1.3.2

# OCR (if using Excavator Detection script)
easyocr>=1.7.0

# Excel automation (if using xlwings in old scripts)
xlwings>=0.30.0

# Signal processing (for idling detection)
scipy>=1.10.0

# Plotting (for visualization)
matplotlib>=3.7.0

# Optional but recommended
pillow>=10.0.0
scikit-learn>=1.3.0