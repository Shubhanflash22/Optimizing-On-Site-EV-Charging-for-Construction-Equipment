================================================================================
 SCENARIO 1 (RECEDING HORIZON) - CONSTRAINT-BY-CONSTRAINT: CODE vs MODEL
================================================================================
Compares what the Julia code enforces against the formal model (math_model.tex /
the source "Optimization.pdf"). The window MILP is built in code/4_MCSModel.jl
(function build_window_model); the closed loop is code/5_MPCLoop.jl (run_mpc);
the entry point is code/7_Receding_Horizon_main.jl. The legacy standalone
code/Scenario_1.jl mirrors the same constraints but is NOT used by the main.

Controller: MULTI-DAY, CROSS-DAY receding horizon. Each 15-min window spans the
rest of today + `lookahead_days` future daytime blocks (global index
k = (day-1)*n_day + w). `n_days` reported days are simulated PLUS one dropped
BUFFER day. Nights link day-blocks: MCS reset to full + parked at grid; CEV SOE
and unfinished work carry over.

Legend:  OK = matches exactly   ADAPTED = deliberate, documented   ~OK = up to a tol.

--------------------------------------------------------------------------------
 Eq.   | Model requirement                    | Code                           | Status
--------------------------------------------------------------------------------
 (1)   | Objective = energy + carbon + NC/OP  | obj in build_window_model:     | OK
        | demand + missed-work slack + towing  | price*P_ch + carbon + demand   |
        | labour                               | + rho_miss*s_miss + rho_labor  |
--------------------------------------------------------------------------------
 (2-5) | Power aggregation, grid-only charge, | P_ch_tot/P_dch_tot defs;       | OK
        | site-only discharge, plug limits,    | P_MCS_CEV<=DCH_plug*rho;       |
        | charge-only-while-idle               | mu<=u[idle] (E4)               |
--------------------------------------------------------------------------------
 (6)   | MCS SOE dynamics w/ efficiency        | SOE_MCS recursion + eta        | OK
        | CEV SOE dynamics                      | SOE_CEV recursion              |
        | NIGHT: MCS reset full; CEV carries    | night_eve reset; CEV single    | ADAPTED
        |                                       | continuous link                | (two-phase)
--------------------------------------------------------------------------------
 (7)   | SOE bounds [min,max]                  | @constraint on SOE_MCS/SOE_CEV | OK
--------------------------------------------------------------------------------
 (8a)  | MCS energy-neutral over cycle         | closed OVERNIGHT (Phase 2),    | ADAPTED
        |                                       | not inside daytime MILP        | (deliberate)
 (8b)  | CEV energy-neutral terminal           | DAILY: SOE_CEV[eve+1] >=       | ADAPTED
        |                                       | SOE_ini - term_tol at EVERY    | (daily
        |                                       | 18:00 (not just horizon end)   | realignment)
--------------------------------------------------------------------------------
 (10)  | Routing: presence partition, arrival/ | z + y partition; beta_arr/dep; | OK
        | departure, flow conservation, travel  | flow conservation; L=k_trv;    |
        | energy; carried-in transit            | is_carried_trv / carried_arr   |
 (10e) | MCS parked at grid by day-end         | z[grid, eve] == 1 (every 18:00)| OK
--------------------------------------------------------------------------------
 (11)  | One activity per assigned CEV; work   | sum_a u == A; work-power <=    | OK
        | capped by availability R              | R_work*(1-mu)                  |
--------------------------------------------------------------------------------
 (12c) | Finish work or pay rho_miss (SOFT)    | s_miss_dig/load per (site,day),| OK +
        |                                       | CUMULATIVE (rolls over) LOWER; | HARD CAP
        | NO WORKING AHEAD (added)              | + HARD upper cap done<=quota   | (added)
--------------------------------------------------------------------------------
 (12d) | Precedence: cum load <= scale*cum dig | s_prec pinned to 0 (HARD);     | OK
        |                                       | within-day; seeded by cum_*_e  |
--------------------------------------------------------------------------------
 (12e) | Rest rule: <= t_limit_rest work per   | <= rest_cap work per (rest_cap | OK
        | rolling (t_limit+dt) window           | +1) window; SEEDED from applied| (history-
        |                                       | work_hist -> holds across the  |  seeded)
        |                                       | 15-min re-solves & at day-end  |
--------------------------------------------------------------------------------
 (13)  | Travel pacing: cum travel tied to     | s_pace_hi/lo pinned 0 (HARD);  | OK
        | cum productive work, two-sided band   | kappa_wt band, seeded by cum_* |
--------------------------------------------------------------------------------

EXTRA CONSTRAINTS IN THE CODE (beyond Eqs. 1-13)
--------------------------------------------------------------------------------
 E1  Peak-demand trackers  | P_NC/P_OP >= grid draw            | realises demand terms of (1)
 E3  Closed-loop carry-in  | each re-solve starts from measured| MPC init. conditions
                           | SOE / position / work done        |
 E4  Charge-only-while-idle| mu <= u[idle]                     | implements (5)-type rule
 E5  Site-visit switches   | require_site_visit / single_visit | optional; OFF by default
 E6  Graceful fallback     | hard-infeasible window -> re-solve| avoids dead hold; a
                           | soft_term+soft_reserve (Softened);| residual shortfall shows
                           | only then hold state (Infeasible) | as a penalty (5_MPCLoop.jl)
 E7  CEV keep-up reserve   | per-boundary SOE floor, built     | makes the DAILY hard 8b
                           | backward from EACH day's 18:00;   | recursively feasible
                           | net charge rate + idle-tail drain | (4_MCSModel.jl)
 E8  Over-charge cap       | SOE_CEV <= SOE_ini + overcharge_  | no "charging ahead":
                           | frac*(SOE_max-SOE_ini)            | stops the MCS hoarding
                           | (always on; upper bound)          | time at the near CEV
 E9  Daytime health floor  | SOE_CEV >= SOE_ini - drawdown_kwh | forces the single MCS
                           | at every daytime boundary         | to SHUTTLE & keep BOTH
                           | (relaxed under soft_reserve)      | CEVs healthy (4_MCSModel)

THE ONLY DELIBERATE DEVIATIONS
--------------------------------------------------------------------------------
 1. (8a)/(10e) TWO-PHASE SPLIT - MCS neutrality + "home by end" are closed
    overnight (deterministic Phase 2) instead of inside one 24h MILP.
 2. (8b) DAILY REALIGNMENT + term_tol=0.1 kWh - the CEV terminal fires at every
    18:00 (each reported day energy-neutral), with a tiny margin that absorbs
    online-estimator drift (exact at term_tol=0, but that risks infeasibility).
 3. (12c) HARD "no working ahead" cap added on top of the soft cumulative target,
    so a day cannot borrow a future day's work (catch-up of leftover still allowed).
 4. ANTI-HOARDING pair (E8 over-charge cap + E9 daytime health floor): with ONE
    MCS serving two far-apart CEVs, the deadline-only reserve (E7) still permitted
    the MCS to camp at the near CEV (over-charging it toward SOE_max) while the far
    CEV drained to near-minimum and was rescued by a last-minute dash. E8 forbids
    charging a CEV far above its start level; E9 forbids any CEV draining more than
    drawdown_kwh below its start level. Together they force the MCS to shuttle.

CLOSED-LOOP CORRECTNESS (modular code 4_MCSModel.jl / 5_MPCLoop.jl):
 - Rolling rules (rest 12e, precedence 12d, pacing 13) are seeded from the current
   day's applied Work/Break history, so a within-window limit can no longer "leak"
   work-runs across the every-15-min re-solves (the 8-in-a-row artefact).
 - The keep-up reserve (E7) makes the daily CEV terminal (8b) recursively feasible;
   the anti-hoarding pair (E8/E9) makes the single MCS SHUTTLE between the two CEVs
   instead of camping at one and neglecting the other.
 - GRACEFUL FALLBACK (5_MPCLoop.jl): if a hard window is still infeasible (early-day
   estimator bias + single-MCS sharing), the loop re-solves ONCE with soft_term=true
   and soft_reserve=true (E7/E9 dropped, terminal penalised) so the MCS still charges
   toward target rather than freezing. Such windows are counted as Softened_windows.
 LATEST RUN (defaults: time_limit_sec=60, mcmc_samples=500, n_days=2 + buffer):
   * :input     -> 0 softened, 0 infeasible; missed work 0.00 h; loop ~53 s.
   * :synthetic -> 3 softened (early-day wind-down), 0 infeasible; missed work
                   0.00 h; loop ~793 s.
 VERIFIED per-row: rest rule, precedence, pacing, SOE bounds, MCS end-at-grid, and
 per-day work all hold; both CEVs shuttle-served and end at/above their start SOE
 (CEV1 no longer over-charged to ~86; CEV2 no longer drained to ~32). The 3 softened
 windows carry NO missed work and still meet the daily terminal (graceful, not lost).
